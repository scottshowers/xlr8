{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
```

And update Procfile to handle installs:
```
web: cd backend && pip install -r requirements.txt && cd /app/frontend && npm install && npm run build && cd /app && python3 -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT
