# Test dependencies for XLR8
# Install with: pip install -r requirements-test.txt

pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
httpx>=0.24.0

# Already in main requirements but needed for tests
pandas>=2.0.0
