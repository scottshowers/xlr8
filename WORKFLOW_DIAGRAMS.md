# 🗺️ XLR8 Workflow Diagrams

## Quick Reference Visual Guides

---

## Workflow 1: Basic PDF Parsing

```
┌─────────────────────────────────────────────────────────────┐
│                    BASIC PDF PARSING                        │
└─────────────────────────────────────────────────────────────┘

Step 1: Upload
┌──────────────┐
│  Upload PDF  │ → Pay register file (text-based)
└──────────────┘

Step 2: Configure
┌──────────────────┐
│ Parsing Options  │ → ☑ Auto-detect fields
└──────────────────┘   → ☑ Extract all tables

Step 3: Parse
┌──────────────┐
│  Parse PDF   │ → Click button, wait 5-30 sec
└──────────────┘

Step 4: Review
┌──────────────────┐
│ Review Results   │ → Check detected fields
└──────────────────┘   → Verify data accuracy

Step 5: Export
┌────────────────┐
│ Download Excel │ → Multi-sheet workbook
└────────────────┘   → Ready for UKG import

⏱️ Time: 2-5 minutes
✅ Best for: Standard formats
```

---

## Workflow 2: Custom Mapping (First Time)

```
┌─────────────────────────────────────────────────────────────┐
│              CUSTOM MAPPING (INITIAL SETUP)                 │
└─────────────────────────────────────────────────────────────┘

Step 1: Upload
┌──────────────┐
│  Upload PDF  │ → Customer-specific format
└──────────────┘

Step 2: Generate Template
┌──────────────────────────┐
│ Generate Mapping Template│ → Analyzes PDF structure
└──────────────────────────┘   → Identifies columns

Step 3: Download Editor
┌────────────────────────┐
│ Download HTML Editor   │ → mapping_editor.html
└────────────────────────┘   → mapping_config.json

Step 4: Edit Offline
┌─────────────────────┐
│ Open HTML in Browser│ → Review columns
└─────────────────────┘   → Map to target fields
                            → Select from dropdowns

Step 5: Save Config
┌──────────────────────┐
│ Download Config JSON │ → customer_mapping.json
└──────────────────────┘   → Save for reuse

Step 6: Upload Mapping
┌──────────────────────┐
│ Back to XLR8         │ → ☑ Use custom mapping
└──────────────────────┘   → Upload JSON file

Step 7: Parse with Mapping
┌──────────────────────┐
│ Parse PDF            │ → Applies custom mapping
└──────────────────────┘   → Uses your field names

Step 8: Export
┌─────────────────────┐
│ Download Excel      │ → Properly mapped data
└─────────────────────┘   → UKG-ready format

⏱️ Time: 15-20 minutes (first time only)
✅ Best for: Custom formats, repeat customers
```

---

## Workflow 3: Reusing Configuration

```
┌─────────────────────────────────────────────────────────────┐
│              REUSING EXISTING MAPPING                       │
└─────────────────────────────────────────────────────────────┘

Step 1: Upload
┌──────────────┐
│  Upload PDF  │ → Same customer, new period
└──────────────┘

Step 2: Load Mapping
┌──────────────────────┐
│ ☑ Custom mapping     │ → Upload saved JSON
└──────────────────────┘   → customer_mapping.json

Step 3: Parse
┌──────────────┐
│  Parse PDF   │ → Instant mapping
└──────────────┘

Step 4: Export
┌────────────────┐
│ Download Excel │ → Consistent format
└────────────────┘   → Same field names

⏱️ Time: 1-2 minutes
✅ Best for: Repeat processing
```

---

## Decision Tree: Which Workflow?

```
Start Here
    │
    ├─ Is this a standard pay register format?
    │   ├─ YES → Use Workflow 1 (Basic Parsing)
    │   └─ NO  → Continue below
    │
    ├─ Do you have an existing mapping for this customer?
    │   ├─ YES → Use Workflow 3 (Reuse Config)
    │   └─ NO  → Continue below
    │
    ├─ Is this a new customer with custom format?
        └─ YES → Use Workflow 2 (Custom Mapping)
                 Then save config for future use!
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                       DATA FLOW                             │
└─────────────────────────────────────────────────────────────┘

PDF Document
    │
    ▼
┌──────────────────┐
│  Upload to XLR8  │
└──────────────────┘
    │
    ▼
┌──────────────────┐      ┌─────────────────┐
│  pdfplumber      │      │  Custom Mapping │
│  Extracts Tables │ ◄───►│  (if provided)  │
└──────────────────┘      └─────────────────┘
    │
    ▼
┌──────────────────┐
│  Detect Fields   │ → Identify payroll fields
└──────────────────┘   → Match patterns
    │
    ▼
┌──────────────────┐
│  Apply Mapping   │ → Rename columns
└──────────────────┘   → Structure data
    │
    ▼
┌──────────────────┐
│  DataFrame       │ → Clean, organized data
└──────────────────┘   → Multiple tables if needed
    │
    ├─────────────────┐
    │                 │
    ▼                 ▼
┌─────────┐      ┌─────────┐
│  Excel  │      │   CSV   │
└─────────┘      └─────────┘
    │                 │
    ▼                 ▼
┌─────────────────────────┐
│      UKG Import         │
└─────────────────────────┘
```

---

## Mapping Editor Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   MAPPING EDITOR                            │
└─────────────────────────────────────────────────────────────┘

XLR8 App
    │
    │ Generate Template
    ▼
┌──────────────────┐
│  JSON Config     │ → Contains all detected columns
└──────────────────┘   → Auto-detected mappings
    │                  → Document info
    │ Download
    ▼
┌──────────────────┐
│  HTML Editor     │ → Open in browser (offline)
└──────────────────┘
    │
    │ Interactive Editing
    ▼
┌─────────────────────────────────────────────────┐
│  Mapping Table                                  │
│  ┌────────────────┬─────────────────┬─────────┐│
│  │ Source Column  │ Target Field    │ Status  ││
│  ├────────────────┼─────────────────┼─────────┤│
│  │ EMP ID         │ employee_id     │   ✓     ││
│  │ Name           │ employee_name   │   ✓     ││
│  │ Gross          │ gross_pay       │   ✓     ││
│  │ Custom Col 1   │ [Select▼]       │         ││
│  └────────────────┴─────────────────┴─────────┘│
└─────────────────────────────────────────────────┘
    │
    │ Save Configuration
    ▼
┌──────────────────┐
│  Updated JSON    │ → With your custom mappings
└──────────────────┘   → Ready to upload
    │
    │ Upload to XLR8
    ▼
┌──────────────────┐
│  Parse with      │ → Uses your field names
│  Custom Mapping  │   → Consistent output
└──────────────────┘
```

---

## Team Collaboration Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  TEAM WORKFLOW                              │
└─────────────────────────────────────────────────────────────┘

Consultant A (Configuration Creator)
    │
    │ 1. Creates mapping for Client X
    │
    ▼
┌───────────────────────┐
│ client_x_mapping.json │
└───────────────────────┘
    │
    │ 2. Saves to shared location
    │
    ▼
┌───────────────────────┐
│  Team Drive/Folder    │
└───────────────────────┘
    │
    │ 3. Shares with team
    │
    ├──────────────────┬──────────────────┐
    │                  │                  │
    ▼                  ▼                  ▼
Consultant B      Consultant C      Consultant D
    │                  │                  │
    │ 4. Download mapping
    │                  │                  │
    ▼                  ▼                  ▼
Uses for          Uses for          Uses for
Client X          Client X          Client X
Week 2 data       Week 3 data       Week 4 data

Result: Consistent processing across entire team!
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  ERROR HANDLING                             │
└─────────────────────────────────────────────────────────────┘

PDF Upload
    │
    ▼
┌──────────────┐
│ Validation   │
└──────────────┘
    │
    ├─ File type OK? ──NO──► Error: "Wrong file type"
    │       │
    │      YES
    │       │
    ├─ File size OK? ──NO──► Error: "File too large"
    │       │
    │      YES
    │       │
    ├─ PDF readable? ──NO──► Error: "Corrupted PDF"
    │       │
    │      YES
    │       │
    ▼
┌──────────────┐
│   Parsing    │
└──────────────┘
    │
    ├─ Tables found? ──NO──► Warning: "No tables detected"
    │       │                        │
    │      YES                       │
    │       │                        ▼
    │       │                  Try different PDF
    │       │                  or check format
    │       │
    ▼       ▼
┌──────────────┐
│  Success!    │ → Continue to export
└──────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                  DEPLOYMENT STACK                           │
└─────────────────────────────────────────────────────────────┘

Developer/Team
    │
    │ Push code
    ▼
┌──────────────┐
│   GitHub     │ → Source code repository
└──────────────┘   → Version control
    │
    │ Auto-deploy
    ▼
┌──────────────┐
│   Railway    │ → Cloud platform
└──────────────┘   → Builds & deploys
    │              → Provides URL
    │
    │ HTTPS
    ▼
┌──────────────┐
│  Users       │ → Access via browser
└──────────────┘   → Upload/process files
                   → Download results
```

---

## Configuration Library Structure

```
Recommended Folder Organization:

📁 XLR8 Configurations/
│
├── 📁 By Customer/
│   ├── acme_corp.json
│   ├── global_industries.json
│   └── techcorp_weekly.json
│
├── 📁 By Format/
│   ├── adp_standard.json
│   ├── paychex_format.json
│   └── quickbooks_payroll.json
│
├── 📁 Templates/
│   ├── standard_payroll.json
│   └── custom_fields_template.json
│
└── 📄 mapping_notes.txt
    (Document which customers use which configs)
```

---

## Processing Time Comparison

```
┌─────────────────────────────────────────────────────────────┐
│                  TIME SAVINGS                               │
└─────────────────────────────────────────────────────────────┘

Manual Data Entry:
█████████████████████████████████████ 60 minutes

Basic OCR:
████████████████████ 30 minutes

XLR8 (First Time):
███████ 10 minutes

XLR8 (With Saved Mapping):
█ 2 minutes

Time Saved: 58 minutes per PDF!
```

---

## Success Checklist Visual

```
Pre-Deployment:
□ Code uploaded to GitHub
□ Railway configured
□ Domain generated
□ Start command set

Post-Deployment:
□ App loads successfully
□ Can upload PDFs
□ Parsing works
□ Mapping editor downloads
□ Excel export works

Team Readiness:
□ Documentation shared
□ Training completed
□ Sample configs created
□ Workflow established
```

---

These diagrams provide quick visual references for understanding and explaining XLR8 workflows!

**Save this file for quick reference during training and onboarding.**
